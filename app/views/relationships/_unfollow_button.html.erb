<%= form_for(current_user.active_relationships.find_by(followed_id: @user.id), html: { method: :delete, class: "mt-3" }) do |f| %>
	<%= f.submit "Unfollow", class: "btn btn-lg btn-secondary btn-block" %>
<% end %>
