<%= form_for(current_user.active_relationships.build, html: { class: "mt-3"}) do |f| %>
	<%= f.hidden_field :followed_id, :value => @user.id %> <!--WRONG-->
	<%= f.submit "Follow", class: "btn btn-lg btn-primary btn-block" %>
<% end %>
